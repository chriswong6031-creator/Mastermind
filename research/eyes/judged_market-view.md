{
  "piece": "brain/market_view.py \u2014 THE perception assembler (charter P1/P5/P7): one deterministic, freshness+confidence-stamped market view assembled from published artifacts, disagreement first-class, consumed by posture/seats/runlog/dashboard, contract at data/market_view/<date>.json",
  "scores": [
    {
      "angle": "Design 1 \u2014 Minimal-composable normalizer over the already-vendored regime file; validated-only tilt + posture_floor_defense lock",
      "sight": 9,
      "testability": 9,
      "compose_risk": 7,
      "total": 25,
      "critique": "The strongest sight case because its central factual claim is TRUE (verified): every incident plane \u2014 risk_radar, vol_regime, froth_fragility, cross_asset, market_gamma, sector_rs, turning_point \u2014 is already embedded in vendor/macro/data/regime/latest.json, so day-1 coverage is 10-11 planes, not 6-7, with zero new fetch surface. The worked incident is grounded in real replayed numbers (budget 0.449, DEF_SLEEVE 7%\u2192~23%, XLK pos 80.8), the phase2 wiring line numbers are accurate, and the validated-vs-advisory hard split (only rotation/cycles-entry/radar can sign the tilt) is the correct P3 posture. Testability is best-in-class: incident replay fixture (06-24 soft / 07-01 hard), calm-tape zero-drift ablation, golden key-order, per-plane freshness fail-closed, 2-4wk shadow \u2014 all realistic because the inputs are one file. The weakness is compose: it feeds THREE consumers (budget shrink term, DEF_SLEEVE floor, derisk escalation) from one disagreement signal. The 'budget owns confidence/transition, view owns disagreement' orthogonality argument is plausible but the disagreement tilt is literally W-I item 4 (rotation-evidence budget term) \u2014 if both ship, that signal is consumed twice; and its ablation only proves zero-drift on AGREEING tape, not bounded stacking on disagreeing tape. Minor nit: quotes radar 75.4 where the incident logs 91/100 growth-scare (different day/field; not load-bearing)."
    },
    {
      "angle": "Design 2 \u2014 Compressed sketch: planes\u2192PlaneRecord, disagreement{splits,posture_bias}, budget_ref read-only",
      "sight": 7,
      "testability": 6,
      "compose_risk": 6,
      "total": 19,
      "critique": "Directionally identical to the winner (above regime_frame, contradiction not averaged, incident 06-24/07-01 targets, budget_ref read-only is a genuinely good idea the others should steal as an audit field) but too thin to score higher: no plane authority mechanics, no normalization rules, self-flagged 'unverified split_min thresholds', and a declared dependency on W-I planes landing that the other two avoid by reading the embedded regime blocks. Compose is its weakest leg: it feeds sev+1/eff_cap 0.55 AND a DEF_SLEEVE floor AND flags the SMH buy from one posture_bias with no stated anti-stacking discipline, no calm-tape-only-vs-disagreeing-tape distinction, and no ablation spec beyond a drift-check. Effort 'L' for this little specification is also not credible. Useful as a checklist, not as the design."
    },
    {
      "angle": "Design 3 \u2014 Separate honest layer above regime_frame; discrete-direction voting, adaptive per-plane skill weights, budget-byte-identical purity",
      "sight": 7,
      "testability": 7,
      "compose_risk": 9,
      "total": 23,
      "critique": "The cleanest compose story of the three and the source of the winning grafts: NEVER touches budget() (guarded by a byte-identical-with/without-view test), only an additive headroom-bounded def_floor_bump plus a subtract-only severity HOLD; absent planes cannot manufacture consensus; degraded views can only refuse to loosen; stale planes are shown-but-not-voting; validated planes auto-DEMOTE on a rolling rank-IC falsifier. Discrete direction tokens (risk_off/neutral/risk_on) that never average into mush is also right. But sight is dented by a verified factual error: it declares vol/gamma, froth, attention and spillover ABSENT from the sparse vendored set when vol_regime, market_gamma, froth_fragility and cross_asset are embedded in the vendored regime latest.json \u2014 its v1 drops the incident's CONCENTRATED and narrowing-top reads that Design 1 keeps. Testability is dragged by the adaptive skill_mult machinery: nightly grading against 21-day falsifiers needs effective_n\u22658 resolved calls, which cannot accrue inside a 4-week gate window \u2014 the cold-start pin makes it inert scaffolding at exactly the moment it's being 'validated', pure extra test surface for zero near-term sight. Holdings-level distribution_tells correctly excluded (book-specific, stays in the escalator)."
    }
  ],
  "winner": "Design 1 (minimal-composable normalizer over the vendored regime file), with Design 3's compose boundary and demotion falsifier grafted in, and Design 2's budget_ref audit field",
  "synthesis": "RECOMMENDED DESIGN \u2014 market_view v1 = Design 1's body with Design 3's spine.\n\nCore (from Design 1, verified against the repo): brain/market_view.py, deterministic view(region)->dict, zero LLM, sits strictly ABOVE brain/regime_frame.py (frame/cycles/budget stay the one regime reader). Eleven planes normalized to PlaneRecord{reading, direction, magnitude, freshness{asof, age_sessions, stale}, confidence, status: validated|advisory, source_contract, raw}, ~90% assembled from blocks ALREADY embedded in vendor/macro/data/regime/latest.json (risk_radar, vol_regime, market_gamma, froth_fragility, cross_asset, market_drivers, dislocation, turning_point, macro_risk, sector_rs, freshness) plus sector_cycles.json via cycles() and the W-I regime_nowcast/liquidity_quality when present. Per-plane freshness computed from each block's OWN embedded asof, never file mtime. Un-vendored dashboard products (subsector_rotation RRG, group_flow, event calendar, intl spillover) ship as null advisory PlaneRecords with a dashboard-handoff note \u2014 the bot never blocks on the dashboard.\n\nDisagreement layer (Design 1's mechanics + Design 3's tokens): directions are discrete risk_off/neutral/risk_on; net_posture_tilt is built ONLY from status=='validated' planes (rotation, cycles-entry/Topping, radar \u2014 the three with walk-forward/forward-logged evidence per the reaudit and post-mortem); advisory planes annotate and shrink but can never sign the tilt. disagreements[] carries named pairwise splits with label_vs_planes {label_direction, plane_consensus_direction, conflict, magnitude} as the load-bearing TOP-LEVEL field \u2014 on the 2026-07-01 replay: label risk_on at confidence 0.327 vs cycles/rotation/radar risk_off, conflict=True, coherence\u22480.38, posture_floor_defense=True. Absent/stale planes contribute weight 0 and can never lower disagreement (no manufactured consensus); coverage<floor \u21d2 degraded view that can only refuse to loosen, never tighten offense on thin data (P2). Deterministic templated brief {what_changed (diff vs yesterday's json), whats_rotating, wheres_the_risk, posture_implication}; optional Sonnet polish gated OFF by env, display-only, never re-read by any decider.\n\nTHE DECISIVE GRAFT (from Design 3) \u2014 compose boundary: v1 does NOT feed regime_frame.budget(). Design 1's three-consumer fan-out (budget + sleeve + severity) from one disagreement signal is the W2 anti-compounding lesson waiting to recur, and its disagreement tilt IS W-I item 4 (rotation-evidence term) \u2014 one signal, one home. market_view becomes the single home of rotation-evidence, feeding exactly two bounded consumers: (1) rotation.build_def_sleeve() gains view_conflict/posture_floor_defense as an ADDITIVE, headroom-bounded fragility term (this closes the counterfactual's documented gap: DEF_SLEEVE 7.1% \u2192 ~23% without trusting the lying label); (2) bot/derisk.py severity escalator gains a subtract-only HOLD (a validated disagreement forbids severity-cap release; it never raises a cap). A permanent guard test asserts budget() output is byte-identical with and without a view present. Also grafted: Design 3's auto-demotion falsifier (a validated plane whose rolling 60-session rank-IC vs SPY forward drawdown goes \u22640 auto-demotes to advisory) and stale-shown-but-not-voting display semantics. From Design 2: a budget_ref field embedding regime_frame.budget()'s output read-only, with a drift-check, so the artifact is a complete posture audit record. EXPLICITLY DEFERRED from Design 3: the adaptive skill_mult ledger (cannot accrue effective_n\u22658 inside any 4-week gate; authority_base + freshness is enough for v1; revisit once the forward log has months of resolved calls).\n\nContract: data/market_view/<date>.json + latest.json, schema market_view.v1 {schema_version, region, asof, built_at, seq (within-day tick counter), planes{11\u00d7PlaneRecord}, net_posture_tilt, coherence, disagreements[], label_vs_planes, posture_floor_defense, posture_confidence, assembly{total,fresh,stale,missing,degraded}, budget_ref, brief, brief_llm?}. Atomic tmp+os.replace writes; golden key-order test (it feeds prompts).\n\nPHASED SHIPPING:\n- Phase 0 (days 1-3, no behavior change): assembler + contract + 11 adapters over the embedded blocks + disagreement layer + deterministic brief + runlog step at the top of phase2 (P5: perception logged before position). Test battery lands WITH it: incident replay fixture (asserts conflict=True, plane_consensus risk_off, n_validated_agree\u22652 on 07-01; soft on 06-24), calm-tape invariance (high-confidence agreeing day \u21d2 conflict=False, zero downstream drift), budget-byte-identical guard, missing-file no-op, per-plane freshness fail-closed.\n- Phase 1 (week 1): read-only consumers \u2014 pm_conviction/strategist/bot_mcp get_regime replace the ad-hoc 7-key regime slice with brief + PlaneRecords + label_vs_planes (this IS W4's prompt edits from one contract; the SMH-rebuy seat prompt now says '3 validated planes dissent, no semis seed'); site/market_view.html mirror off the artifact; overnight wake re-assembles fast planes with seq++ and a new/flipped VALIDATED disagreement joins gate.state_signature.\n- Phase 2 (weeks 2-4, shadow \u2192 live): the two binding consumers (DEF_SLEEVE view_conflict bump; derisk severity hold) run in SHADOW posture logging would-have-done deltas; promote to live only on green replay battery + zero-drift calm-tape ablation + shadow showing no compounding on disagreeing tape (P8).\n- Phase 3 (post-gate, separate decisions): (a) reconsider a budget cross-plane term ONLY if shadow shows the sleeve+severity path under-responds, as a replacement for \u2014 never an addition to \u2014 W-I item 4; (b) advisory\u2192validated promotions via the pre-registered AUC>0.55 forward-log gate; (c) dashboard handoff to extend the vendoring manifest (group_flow, subsector RRG, event calendar, intl spillover) so the null planes fill in; (d) the adaptive skill ledger once effective_n clears MIN_N honestly."
}