{
  "piece": "CONTESTED DESIGN 2 \u2014 the ANTICIPATION BATTERY (BUBBLE-FORMATION / CRASH-RISK / SECTOR-TOP graded alarms composed from published dashboard products, consumed exactly once by the budget/DEF_SLEEVE/severity machinery)",
  "scores": [
    {
      "angle": "Design 1 \u2014 MINIMAL-COMPOSABLE (brain/anticipation.py over already-vendored artifacts; one evidence term into rotation.fragility_signal; reserved CRITICAL crash-risk notch)",
      "sight": 9,
      "testability": 9,
      "compose_risk": 8,
      "total": 26,
      "critique": "Every load-bearing factual claim verified in the repo: the SMH osc roll (98.3\u219281.5), Peak/Topping pin, osc_slope \u221216.5, and the 06-22 provisional major-peak turn are all LIVE in the vendored sector_cycles.json baskets block \u2014 so SECTOR-TOP crossing WATCH ~06-12, ELEVATED ~06-19, CRITICAL 06-22..06-25 is the earliest sight of the three, and it fires from data the bot already syncs (zero vendoring precondition). Its decisive insight matches the counterfactual exactly: fragility_signal=0.2019 because dwell(risk_on)=0 and STABLE killed the weakening bump; a structure-derived 0.85 term lifts it to ~0.50 \u2192 def budget 7%\u2192~17.5%, funding the XLV/XLU/XLP rotation the STABLE label suppressed. The worked math is honest (clamp, w_antic=0.35). Testability is the best of the three: byte-identical defaults (optional arg default 0.0, env flag OFF), reuses the calibration/shadow-books seam and the W-I AUC>0.55 bar, and SECTOR-TOP already carries the sector_cycles walk-forward (Peak \u221239bp/20d, honestly labeled weak-advisory). Compose is nearly clean \u2014 ONE new term in ONE existing equation, budget() deliberately untouched, notch clamped +1 and flag-gated. Two docked points: (a) the W-I overlap (distribution_escalator and rotation-evidence budget term use the same defensive-RS/distribution structure) is only handled by a 'coordinate before build' note, not a mechanical dedup \u2014 Design 2 solves this better with dedup_key max(); (b) gex feeds both crash-risk (\u2192fragility term) and derisk severity \u2014 different output surfaces (def sleeve vs gross cap), but the exactly-once story should state that rule explicitly. Sight docked one point only because the earliest crossings (WATCH 06-12) ride the weakest validated leg, so the EARLY part of the early warning is the least trustworthy part \u2014 correctly priced by its own advisory tiering."
    },
    {
      "angle": "Design 2 \u2014 INSTITUTIONAL early-warning wall (anticipation_battery.py over the full published contract surface; budget_signal shrink_mult + def_floor_bump; validated-legs-only notch with dedup)",
      "sight": 8,
      "testability": 8,
      "compose_risk": 7,
      "total": 23,
      "critique": "Broadest and most honest plane coverage \u2014 it is the only design that gives BUBBLE-FORMATION a real crowding artifact and CRASH-RISK real credit/auction legs, and its authority tiering is the best P3 work in the set: validated-today claims cite artifacts that actually exist and self-grade (froth_fragility log.jsonl carries graded/hit/realized; risk_radar forward_log carries drawdown_prob h21 \u2014 the exact 0.16\u21920.19 read the autopsy used; group_flow validation_meta is DSR-bearing), and it correctly demotes vol_regime using the dashboard's OWN gate (live_overlay_helps=false). The dedup_key max() rule against distribution_tells' own +1 escalator at derisk.py:191 is the single best anti-double-count mechanism proposed anywhere \u2014 Design 1 needs it. The radar-floor rule directly fixes the incident's 'bot never saw the caution' root cause. But sight crossings are later than Design 1 (WATCH 06-24, ELEVATED 06-30, CRITICAL 07-01 \u2014 1 day before the break vs Design 1's ~week), and most of its legs are NOT in the bot's vendored set today (verified: no froth/dislocation/group_flow/auctions vendored; breadth empty) \u2014 so day-1 sight degrades to roughly Design 1's legs anyway, with the full wall gated on a sparse-manifest extension it lists only as a precondition. Compose docked: THREE touch surfaces (budget shrink_mult, DEF floor bump, severity notch) versus Design 1's one-plus-notch; shrink_mult 0.75\u20131.0 multiplies into the exact compounding-shrink stack the charter names as risk #1; and the residual-legs rule excludes liquidity_quality/distribution_tells but NOT gex, which would then be consumed twice on the same gross surface (battery\u2192budget shrink AND derisk\u2192severity cap). Effort L with a cross-repo dependency also makes the 4-week gate window tighter, though walk-forward-on-history and the replay fixture (nice explicit P6 machinery) remain achievable."
    },
    {
      "angle": "Design 3 \u2014 compressed sketch (reuse graded froth/vol_shock, cold-weight 0.5, evidence into budget+rotation, CRITICAL crash notch)",
      "sight": 5,
      "testability": 5,
      "compose_risk": 4,
      "total": 14,
      "critique": "Directionally the same animal as the other two but too thin to trust. Sight claims (XLK ELEVATED 06-24, CRITICAL 07-01) are plausible and consistent with the nowcast dates in signals.md, but no leg is tied to a verified vendored artifact, level definitions are not pre-registered beyond a formula fragment, and 'yahoo READ' implies recomputing MACD/RS legs inside the bot \u2014 duplicating the W-I nowcast rather than consuming published perception (P7 violation risk, and a drift surface P7 exists to kill). Testability: gates are named (CRASH AUC>0.55, XLK replay fixture) but the 'weight by hit-rate' scheme has no defined grading source, no shadow seam, no flag/byte-identical story, and 'cold w=0.5' is an unregistered prior steering evidence from day one \u2014 soft P3. Compose is the real failure: 'evidence into budget+rotation' routes the SAME evidence into two consumers, which is precisely the W2 double-count the mandate forbids; 'evidence not caps' is asserted, not designed; no dedup with the W-I distribution escalator; no freshness/degrade contract beyond a bullet. Nothing here is wrong so much as unfinished \u2014 its only distinct contribution (hit-rate-weighted legs) is subsumed by Design 2's authority tiers."
    }
  ],
  "winner": "Design 1 \u2014 MINIMAL-COMPOSABLE anticipation.py (with Design 2's authority-tier, notch-dedup, and radar-floor grafts)",
  "synthesis": "WINNER: Design 1, because it is the only design whose worked example is fully verified against data the bot vendors TODAY (SMH osc roll + 06-22 provisional peak turn live in sector_cycles.json baskets; mtf_monitor + gex/SPY present; breadth/froth/radar-snapshot confirmed absent), whose sight is earliest (SECTOR-TOP ELEVATED ~06-19, CRITICAL 06-22..25 \u2014 a week-plus before the 07-02 break and ahead of the 07-01 SMH rebuy), and whose consumption path is the tightest (ONE evidence term in rotation.fragility_signal, budget() untouched, one flag-gated notch). It attacks the incident's proven soft spot exactly: the counterfactual shows fragility_signal throttled to 0.2019 by the regressed STABLE/risk_on label; a price/positioning-structure term lifts it to ~0.50 \u2192 def budget 7%\u2192~17.5% without trusting the label that lied (charter P1's independent plane, made mechanical).\n\nGRAFTS FROM DESIGN 2 (mandatory): (1) AUTHORITY TIERS AS CODE \u2014 the notch-eligible set is hard-restricted to legs with on-disk forward grading: risk_radar drawdown_prob (forward_log.jsonl), froth_fragility graded log, group_flow DSR meta; everything else (dislocation, subsector_scan, themes, auctions, vol_regime per its own live_overlay_helps=false gate, and ALL of SECTOR-TOP) is advisory and barred from severity forever-until-gated. (2) NOTCH DEDUP \u2014 the CRITICAL crash-risk +1 and the W-I distribution_escalator +1 write through one seam in bot/derisk with dedup_key/source and max() not sum(); this also resolves Design 1's weakest spot. Extend the same rule to the W-I rotation-evidence budget term: it is REROUTED through the single anticipation_evidence term (one owner for 'structure raises the defensive floor'), not stacked beside it. (3) GEX EXACTLY-ONCE RULE (fixes both designs): derisk OWNS gex for severity; the Battery may read gex only to CLASSIFY crash-risk level, but crash-risk's contribution to the fragility term must be computable from non-gex legs \u2014 gex never shrinks two surfaces. (4) RADAR FLOOR + MANIFEST EXTENSION \u2014 once the sparse manifest vendors the risk_radar snapshot (eb9fd0b lineage), froth log, dislocation state_log, group_flow meta, and auction parquet, the radar state becomes a floor (raises crash-risk, never lowers) and BUBBLE-FORMATION gains its real crowding leg. (5) rs_diff magnitude emitted on SECTOR-TOP (the user's 'exactly how they are rotating and by how much'). (6) The incident replay fixture as a P6 gate: frozen 06-20..07-02 inputs must assert SECTOR-TOP(tech) \u2265ELEVATED by 06-19 and CRASH-RISK \u2265ELEVATED by 06-26, forever.\n\nPHASED SHIPPING: PHASE 0 (week 1, vendored-legs-only, flag OFF, byte-identical): brain/anticipation.py battery()/sector_top()/bubble_formation()/crash_risk() reading sector_cycles baskets + mtf_monitor + gex + anticipationdata; fail-closed degrade tests; publish data/anticipation/<asof>.json once per 22:40 build (P7: all books read the one artifact); inject into the W4 PM payload as advisory perception (P5); land the incident replay fixture. PHASE 1 (weeks 2\u20133, evidence arm): walk-forward CRASH-RISK vs SPY 5d-fwd drawdown 2011\u20132026, AUC>0.55 + fires-on-<10%-of-days sanity; on pass, arm w_antic in fragility_signal (doctrine.yml, tagged unverified-prior) with the W-I rotation-evidence term routed through it; extend calm-tape byte-identical invariance and the W2 offensive-gross floor test to include the Battery; register per-alarm 21d falsifiers in calibration/shadow_books. PHASE 2 (weeks 3\u20134, wall extension): sparse-manifest PR on the dashboard side vendoring froth/radar-snapshot/dislocation/group_flow/auctions; upgrade crash-risk legs to the graded artifacts with Design 2's tiers + radar floor; arm the severity notch (flag ON) only after the AUC gate AND the dedup seam ship together. PHASE 3 (W7 acceptance, +4 weeks shadow): pre-committed kills \u2014 negative shadow Brier vs coin-flip demotes an alarm to display permanently (do-not-build list); ablation must show brake-week saving \u2265 1/3 of trend-week give-up or w_antic reverts to 0; BUBBLE-FORMATION stays advisory until a forward-graded crowding artifact exists. Effort M+; the only cross-repo dependency (manifest) is isolated to Phase 2 so Phases 0\u20131 deliver the incident-preventing sight from day one."
}