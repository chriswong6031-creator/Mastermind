{
  "piece": "CONTESTED DESIGN 3 \u2014 the POSTURE DECIDER (explicit P5 stage: one posture object decided from market_view before name selection, subsuming budget()/fragility_signal()/W-I damp)",
  "scores": [
    {
      "angle": "Design 1 \u2014 hoist-in-place: brain/posture.py keeps budget() and fragility_signal() equations verbatim as internal _offense_term/_defense_term, W-I planes as discrete shrink multipliers, class mapped off the terms, W1-style hysteresis",
      "sight": 7.5,
      "testability": 9,
      "compose_risk": 8,
      "total": 24.5,
      "critique": "The most surgical and best-grounded migration (every line anchor verified: phase2:337-343, rotation:313, derisk:171/245, judgment_book:100/230), and keeping the equations verbatim makes its byte-identity and calm-tape gates genuinely achievable \u2014 the strongest testability story of the three. But sight is structurally the weakest: its offense and defense terms are dominated by (confidence, transition, flip_margin) \u2014 the precise fragments the autopsy shows regressing (WEAKENING\u2192STABLE on the memoryless flag counter, and the bot reading the rosier vendored plane). On 06-26 the arithmetic works because that day still read WEAKENING; had the regression landed a day earlier, only the liquidity plane + hysteresis hold the line, and its own risk #5 concedes the full defensive arithmetic (offense floored, defense 0.26) only materializes once the W-I planes are armed past their gates. Two defects: (a) 'NEW brain/posture.py' collides with the EXISTING display-only posture.py (Brain-book label deriver consumed by app/web.py) \u2014 a real wiring bug the other two designs explicitly avoided; (b) risk_notch enters derisk as additive severity (severity = base + notch) rather than min-composed caps \u2014 argued away but strictly less idempotent than Design 3's min(state_cap, severity_cap, posture_notch_cap)."
    },
    {
      "angle": "Design 2 \u2014 institutional book-of-record: posture_decider.py with a single weighted-SUM posture score over 8 planes, offense_budget re-derived from the score, posture_compliance.py deviation ledger, doctrine.yml weights sized by the W5 E-regression",
      "sight": 9,
      "testability": 7,
      "compose_risk": 7,
      "total": 23,
      "critique": "Best sight mechanics: the 8-plane fused score means the stale Goldilocks/STABLE label is outvoted (06-26 score 0.760 \u2192 PRESERVE; still 0.565 \u2192 ROTATE_DEFENSIVE with all three W-I planes zeroed), and it is the only design that explicitly kills the W-I item-4 rotation-evidence budget term before it ships as a fourth parallel lever. The compliance ledger (posture_deviation rows \u2192 W6 registry probation) is the best book-binding artifact of the three. But the subsumption is mathematically a REPLACEMENT, not a fold-in: offense_budget = clamp(base + slope\u00b7(1\u2212score)) does NOT reproduce budget()'s clamp(0.40+0.20\u00b7conf\u00b7T\u00b7F) \u2014 on a calm tape (conf 0.7, STABLE) the score path yields ~0.58 vs today's 0.54, so the claimed 'shim keeps W2 golden tests green' and 'calm-tape byte-identical' gates are internally inconsistent; passing them requires retuning 8 doctrine weights + bands, which is exactly the kind of new-claim surface P3 makes expensive. That pushes realistic validation past 4 weeks (E-regression sizing of 8 weights + 2015-2026 walk-forward + a give-up kill rule) and creates compose risk: every sleeve number changes on day one even with no stress, violating the shrink-only/degrade-to-today discipline the W2 suite enforces."
    },
    {
      "angle": "Design 3 \u2014 renormalizing multi-plane defense_pressure: posture_decider.py computes D as an availability-weighted MEAN over 8 shrink-biased planes (missing plane drops out, never defaults to 0-risk); offense keeps the identical budget clamp internally; defense/class/notch driven by D; caps min-composed; adaptive weights pinned equal until n\u22658 HAC",
      "sight": 9,
      "testability": 8,
      "compose_risk": 9,
      "total": 26,
      "critique": "The hybrid gets both halves right: the budget clamp lives verbatim inside decide() (inputs read once, calm-tape byte-identity actually holds \u2014 Design 1's virtue), while class/defense come from the renormalizing multi-plane D (Design 2's virtue), so the incident's root cause \u2014 one regressing label suppressing budget() AND fragility_signal() simultaneously \u2014 is structurally outvoted: even force-feeding the bot's rosy STABLE plane (P1,P2\u21920), the mean over the remaining planes is ~0.50, still at the ROTATE-DEFENSIVE floor (knife-edge, and honestly flagged). Cleanest compose story: W-I planes touch the book through exactly one scalar; eff_cap = min of three ceilings (idempotent, can never double-cut); grep-gate single-consumption test is a ship-blocker; fragility_signal survives only as the posture-off fallback path. Weaknesses: the adaptive per-plane weights (rolling forward-IC, EMA decay, HAC n\u22658) are premature machinery that cannot earn authority in 4 weeks and should be amputated from phase 1 \u2014 the design partially concedes this by pinning to equal-weight cold-start and shipping the class mapping on the replay gate, not learned weights; and the 0.50 boundary on the rosy-plane replay means the class bands need the incident fixture to pin them, not hand-tuning. New single-point-of-failure module risk is real but mitigated identically to regime_frame (never-raise, degrade-to-BALANCED, degrade-to-stateless hysteresis)."
    }
  ],
  "winner": "Design 3 \u2014 renormalizing multi-plane defense_pressure (posture_decider.py), with Design 1's verbatim-equation migration discipline and Design 2's compliance ledger grafted in",
  "synthesis": "RECOMMENDED DESIGN \u2014 brain/posture_decider.py (name chosen to avoid the existing display-only brain/posture.py and the W5 posture_governor).\n\nCore = Design 3's split-brain: (1) OFFENSE side \u2014 the W2 budget equation clamp(0.40+0.20\u00b7conf\u00b7T\u00b7F) moves VERBATIM inside decide(); regime_frame.budget() becomes a thin shim returning posture.offense_budget (W2 golden tests + China/HK callers stay green byte-identical \u2014 Design 1's discipline, which Design 2's score-derived offense would have silently broken). (2) DEFENSE/CLASS side \u2014 defense_pressure D = availability-renormalizing weighted MEAN over the shrink-biased planes (regime fragility, transition tilt, flip fragility, W1 dwell, cycles late_cycle-vs-entry_favored, distribution_tells, liquidity_quality, price-action nowcast, risk_radar-when-present), EQUAL-WEIGHT PINNED \u2014 Design 3's adaptive per-plane IC weighting is amputated from v1 (cannot clear P3 in 4 weeks; posture_governor stays the only learner). D drives: class bands (OFFENSE<0.25 / BALANCED / ROTATE-DEFENSIVE\u22650.50 / PRESERVE\u22650.75), defense_floor = DEF_SLEEVE_MAX\u00b7D (fragility_signal dies as a public sizer, survives only as the posture-off fallback), cash_floor, conviction_appetite, risk_notch. This is why it sees the incident earlier than Design 1's structure can: the multi-plane mean outvotes the memoryless WEAKENING\u2192STABLE regression and the rosy vendored plane the bot actually traded on \u2014 the autopsy's P1 lesson made structural.\n\nSUBSUMPTION LEDGER (one consumption per signal, P7): budget() \u2192 shim (equation inside decide()); fragility_signal() \u2192 dies (dwell+conf+weakening are planes of D); W-I rotation-evidence budget term (masterplan W-I item 4) \u2192 NEVER SHIPS separately, it IS planes P5-P7 (Design 2's explicit kill, adopted); derisk \u2192 unchanged as the off-schedule reflex, gains posture_notch_cap via eff_cap = min(state_cap, severity_cap, posture_notch_cap) \u2014 Design 3's min-composition over Design 1's additive severity (idempotent ceilings cannot double-cut). Ship-blocker: grep-gate asserting frame confidence/transition and fragility_signal are read NOWHERE downstream of decide().\n\nHYSTERESIS: escalate same-session, de-escalate needs 3 consecutive lower-band builds AND no sev\u22652 tripwire in 2 sessions (mirrors macro_risk dwell + tripwire clamp), state in data/posture/state.json, degrade-to-stateless on corruption, max-dwell auto-release ported so PRESERVE cannot become a trap. This makes the 07-01 un-cap-to-1.00 structurally impossible.\n\nBOOKS (P7, one view): systematic books mechanical (phase2 lead_budget \u2190 posture.offense_budget at :337-343; build_def_sleeve(target=posture.defense_floor) at :820 with fragility fallback when posture off; conviction NEW entries \u00d7 appetite; eff_cap min-composed). LLM books via the EXISTING directive seam (autonomous.py:248 PRIORITY DIRECTIVE block, judgment_book.build(directive=)) \u2014 binding guidance, bracketed mechanically by engine caps above and floor_legs below. Graft Design 2's brain/posture_compliance.py: post-build realized-vs-target gross per book \u2192 posture_deviation rows in the runlog + data/posture/<asof>/deviations.json, graded through the three-questions Brier duty, feeding W6 registry probation.\n\nINCIDENT ARITHMETIC (the fixture): 06-26 planes (conf 0.25-0.33 WEAKENING, flip<0.15, dwell caution, XLK Peak late_cycle vs XLV/XLU entry_favored, SMH crowding+2W-topped, RRP-exhausted STRESS, def-RS nowcast fired 06-24) \u2192 D\u22480.74 \u2192 ROTATE-DEFENSIVE: offense 0.40-0.45, defense_floor \u22480.22-0.27 (XLV/XLP/XLU/USMV/SGOV \u2014 the book the user held for +7.8/+3.9/+2.4 while the bots held $0.00 defensive), notch +1 \u2192 cap 0.70, appetite ~0.5 blocking the 07-02 SMH rebuy. 07-01's regressed STABLE print drops raw D but de-escalation is dwell-blocked \u2192 class HELD. Rosy-plane stress variant (P1,P2 forced to 0) \u2192 D\u22480.50, still ROTATE-DEFENSIVE \u2014 the band edge is pinned by the fixture, not hand-tuned.\n\nPHASED SHIPPING (\u22644 weeks, default MASTERMIND_POSTURE_DECIDER=0 throughout until gates green): W1 \u2014 module + data/posture/latest.json published SHADOW-ONLY every build with full evidence trail; fixtures land: incident battery (06-24..07-02 \u2192 ROTATE-DEFENSIVE on 06-26; 07-01 de-escalation blocked; 07-02 SMH-rebuy suppressed), calm-tape invariance (conf>0.55/STABLE/benign \u2192 OFFENSE, budgets byte-identical to today), byte-identity control arm (flag off + DEF_SLEEVE_MAX=0 \u2192 current book). W2 \u2014 the subsumption in ONE PR (budget shim, fragility retirement, def_sleeve target param, min-composed eff_cap, grep-gate as CI ship-blocker). W3 \u2014 book fan-out: directive rendering to the 5 LLM/free-form books + posture_compliance ledger + doctrine.yml posture: block (bands + class maps, tagged unverified-prior; W5 E-regression sizes them later against forward SPY 5d max-drawdown). W4 \u2014 arm after all gates green, with W-I planes entering at whatever authority they earned in W-I (shrink-only/advisory until each clears its own gate \u2014 a sub-validated plane can raise D, never lower it). Adaptive plane weights and any learned band tuning deferred to the W5 governor track under its existing HAC/default-OFF discipline."
}